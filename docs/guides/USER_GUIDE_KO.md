# LambLink 한글 사용자 설명서

이 문서는 스트리머가 설치 파일을 내려받은 뒤 **설치 → 치지직 로그인 → OBS 설정 → 시청자 링크 공유 → 방송 전 점검**까지 순서대로 진행할 수 있도록 작성되었습니다.

## 1. 준비 사항

- Windows 10 또는 Windows 11 PC
- Steam판 Cult of the Lamb 설치
- 방송에 사용할 치지직 채널 계정
- OBS Studio
- 인터넷 연결

설치 전에 Cult of the Lamb과 기존 LambLink Companion을 모두 종료하세요. 게임을 실행한 상태에서 설치하면 일부 Mod 파일이 정상적으로 교체되지 않을 수 있습니다.

## 2. 릴리스 파일 내려받기

1. GitHub 저장소의 **Releases**를 엽니다.
2. 가장 최신 릴리스에서 `LambLink-Setup-버전.exe` 파일을 내려받습니다.
3. `source code.zip`이나 `source code.tar.gz`는 개발자용 소스이므로 일반 사용자는 받을 필요가 없습니다.

Windows SmartScreen이 게시자를 확인할 수 없다는 경고를 표시할 수 있습니다. 반드시 이 저장소의 공식 Releases에서 받은 파일인지 확인하고, 릴리스에 SHA-256 값이 제공된 경우 내려받은 파일과 대조한 뒤 실행하세요.

## 3. 프로그램 설치

1. 내려받은 Setup EXE를 실행합니다.
2. 설치기가 Steam 라이브러리에서 Cult of the Lamb 폴더를 자동으로 찾습니다.
3. 표시된 경로가 실제 게임 폴더인지 확인합니다.
4. 경로를 찾지 못했다면 **찾아보기**를 눌러 `Cult Of The Lamb.exe`가 있는 폴더를 선택합니다.
5. **설치**를 누르고 완료될 때까지 창을 닫지 않습니다.
6. 설치 완료 메시지가 나타나면 **Companion 실행**을 누릅니다.

설치기는 BepInEx, COTL_API, 한글 폰트 패치, 게임 Mod와 Companion을 자동으로 설치합니다. 구성요소마다 SHA-256을 확인하므로 다운로드가 손상되거나 배포 파일이 일치하지 않으면 설치를 중단합니다.

### 설치 위치

- 게임 Mod: Steam의 `Cult of the Lamb\BepInEx` 아래
- Companion: `%LOCALAPPDATA%\Programs\LambLink`
- 사용자 설정과 로그: `%LOCALAPPDATA%\LambLink`
- 바탕화면/시작 메뉴: Companion 바로가기

## 4. 치지직 로그인

1. Companion을 실행하면 검은색 콘솔 창이 열립니다.
2. 기본 브라우저에 치지직 로그인·승인 화면이 열립니다.
3. 방송에 사용할 치지직 계정인지 확인하고 접근을 승인합니다.
4. Companion 창에 `RELEASE / CHZZK LIVE`와 연결된 채널 이름이 표시되는지 확인합니다.
5. 이 창은 채팅, 후원, OBS와 게임을 연결하는 프로그램이므로 방송 중에는 종료하지 않습니다.

로그인 브라우저가 자동으로 열리지 않거나 인증이 끝나지 않는다면 Companion을 한 번 종료한 뒤 다시 실행하세요. VPN, 광고 차단 프로그램 또는 방화벽이 로컬 콜백 주소를 막고 있지 않은지도 확인합니다.

## 5. OBS 오버레이 설정

### 자동 복구용 오버레이 파일 확인

OBS와 Companion은 **같은 PC에서 실행**해야 합니다. Companion을 최초 한 번 실행한 뒤 `overlay`를 입력하면 자동 복구용 로컬 파일 경로가 표시됩니다.

```text
C:\Users\사용자명\AppData\Local\LambLink\LambLink-Overlay.html
```

표시된 실제 파일 경로를 사용하세요. 이 파일은 Companion이 꺼져 있어도 OBS에서 열리며, Companion이 실행되면 자동 연결됩니다. 이후에는 OBS와 Companion의 실행 순서가 상관없습니다. 연결이 끊기면 오래된 이벤트를 숨기고 재시도합니다. 서버와 페이지가 정상 응답하는 환경에서 보통 수 초 안에 복구됩니다.

**기존 사용자는 한 번 전환해야 합니다.** `http://127.0.0.1:17883/overlay...`를 URL에 넣었던 소스도 아래의 **로컬 파일** 방식으로 바꾸세요. HTTP URL만 사용하는 소스는 첫 페이지 로드 실패 후 자동 복구를 보장하지 않습니다. 파일을 삭제하거나 다른 PC로 옮기면 다시 설정해야 합니다.

### OBS에 추가

1. OBS의 **소스 목록**에서 `+`를 누릅니다.
2. **브라우저**를 선택합니다.
3. 새 소스 이름을 `LambLink 오버레이`로 지정합니다.
4. **로컬 파일**을 켜고 **찾아보기**로 Companion이 안내한 `LambLink-Overlay.html`을 선택합니다. URL 입력란에 경로를 붙여넣는 방식이 아닙니다.
5. 너비와 높이를 현재 방송 캔버스와 동일하게 설정합니다.

| OBS 기준 해상도 | 너비 | 높이 |
|---|---:|---:|
| 1920×1080 방송 | 1920 | 1080 |
| 2560×1440 방송 | 2560 | 1440 |
| 1280×720 방송 | 1280 | 720 |

6. **사용자 지정 CSS**는 비워 둡니다.
7. **보이지 않을 때 소스 종료**는 끄는 것을 권장합니다. 켜져 있어도 소스를 다시 활성화하면 로컬 파일이 열려 자동 연결됩니다. **소스가 활성화되면 브라우저 새로고침**은 자동 복구의 필수 조건이 아닙니다.
8. 평상시 아무것도 보이지 않는 것이 정상입니다. 오버레이 페이지의 배경은 투명합니다.
9. 소스에 변형이 적용되어 있다면 **변형 초기화**로 캔버스에 맞춥니다. 자르기나 별도 배율은 사용하지 않는 것이 좋습니다.

### 연결 확인

OBS 브라우저 소스를 켠 뒤 Companion에서 `status`를 입력합니다. `OVERLAY=ready`와 `OVERLAY_DOC_CURRENT=True`가 표시되면 페이지가 연결된 상태입니다. 일반 브라우저의 오버레이 탭은 닫고 확인하세요. 이 상태만으로 OBS 장면에 실제 노출되는지까지 판별하지는 못합니다.

`OVERLAY=not-polling`이면 다음을 확인하세요.

- Companion이 계속 실행 중인지
- OBS와 Companion이 같은 PC에서 실행되는지
- **로컬 파일**이 켜져 있고 `overlay`가 안내한 파일이 선택되어 있는지
- 브라우저 소스가 숨김 또는 비활성화 상태인지
- OBS 브라우저 소스의 **현재 페이지 새로고침**을 실행했는지
- 기존 HTTP URL 방식이라면 위의 로컬 파일 방식으로 전환했는지

### 오버레이 표시 위치

로컬 파일 방식에서 전체 배경이 검게 보인다면 OBS 브라우저 소스 속성의 **사용자 지정 CSS**에
다음을 입력하고 **현재 페이지 새로고침**을 누르세요. 내부 페이지와 iframe의 색상 모드를
일치시켜 투명 배경이 검은색으로 합성되는 문제를 해결합니다.

```css
html, body { background: transparent !important; }
iframe { background: transparent !important; color-scheme: dark !important; }
```

수정된 Companion은 같은 처리를 로컬 HTML에 포함합니다. 업데이트 후 Companion을 한 번
실행하고 OBS 소스를 새로고침하면 위 CSS를 별도로 넣지 않아도 됩니다. 일반 브라우저 창을
창 캡처하는 방식이 아니라 OBS의 **브라우저** 소스로 해당 로컬 HTML을 열어야 합니다.

- 추첨과 당첨 결과: 화면 중앙
- 후원 이벤트 카드: 화면 왼쪽 위에서 18px, 최대 너비 480px
- 지속 버프·디버프: 화면 왼쪽부터 오른쪽 방향으로 배치

후원 카드와 추첨 화면의 크기·위치는 서로 독립적입니다. OBS에서 브라우저 소스 자체를 잘라내면 카드가 보이지 않을 수 있으므로 전체 캔버스를 유지하세요.

## 6. 시청자 외형 설정 링크 공유

치지직 로그인이 끝나면 Companion에 `[VIEWER PAGE]` 배너와 스트리머 전용 주소가 표시됩니다. 주소에는 로그인한 스트리머의 채널 ID가 포함되므로 다른 방송의 링크와 바꾸어 사용하면 안 됩니다.

### 가장 간단한 복사 방법

Companion 창에서 `viewer copy`를 입력하면 주소가 Windows 클립보드에 복사됩니다. 직접 열어 확인하려면 `viewer open`, 주소를 다시 표시하려면 `viewer`를 입력합니다.

로그인 완료 후에는 바탕화면에 **CHZZK 시청자 외형 설정 페이지** 바로가기도 생성됩니다.

### 어디에 공유하면 좋은가

- 치지직 채널 공지
- 방송 설명 또는 고정 안내
- 방송 시작 전 채팅 공지
- 봇의 반복 안내 메시지
- 디스코드나 팬카페의 방송 안내 게시물

### 시청자에게 보낼 안내문

아래 문구는 **LambLink v1.0.0 정식 운영 환경**을 기준으로 작성되었습니다. 시청자는 별도 프로그램을 설치할 필요가 없습니다.

> 🐑 **방송 속 신도로 참여해 보세요!**<br>
> 아래 LambLink 시청자 페이지에서 치지직 계정으로 로그인한 뒤 원하는 **형상·색상·종류**를 선택하고 `이 외형으로 저장`을 눌러 주세요. 실제 방송 채널과 연결된 전용 링크이므로 주소의 `?streamer=...` 부분을 지우면 안 됩니다.<br>
> 🎨 외형 설정: `[Companion에서 복사한 시청자 페이지 주소]`<br>
> 게임에서 새 신도 모집이 시작되면 치지직 채팅에 `!신도`를 입력하면 추첨에 참여할 수 있습니다. 당첨되면 저장한 외형과 치지직 닉네임이 게임 신도에게 적용되며, 이름 옆에 초록색 `Chzzk` 표시가 붙습니다.<br>
> 현재 세대의 신도가 살아 있는 동안에는 같은 계정으로 새 신도 추첨에 다시 참여할 수 없습니다. 사망이 확인되면 다음 세대 참여가 다시 열립니다. 시청자 페이지의 `신도 히스토리 보기`에서 생성·사망·부활 기록과 이전 외형을 확인할 수 있습니다.<br>
> ※ 저장하지 않았거나 현재 게임에서 사용할 수 없는 외형은 안전한 기본 외형으로 적용될 수 있습니다.

채팅에 반복해서 올릴 때는 짧은 문구를 사용하세요.

> 🐑 신도 참여: `[시청자 페이지 주소]`에서 치지직 로그인 후 외형을 저장하고, 모집이 시작되면 채팅에 `!신도`를 입력해 주세요! 별도 설치는 필요 없습니다.

링크에 포함된 `?streamer=...` 부분을 지우지 마세요. 시청자는 자신의 치지직 계정으로 로그인해야 본인의 외형을 저장할 수 있습니다.

네이버 카페·디스코드·방송 설명에 바로 붙여 넣을 수 있는 전체 안내문은 **[시청자 참여 안내문](VIEWER_NOTICE_KO.md)**을 사용하세요.

## 7. 게임 실행과 첫 동기화

1. Companion을 실행하고 치지직 로그인을 완료합니다.
2. Cult of the Lamb을 실행합니다.
3. 방송에 사용할 저장 파일을 불러와 실제 플레이 화면까지 들어갑니다.
4. Companion에서 `status`를 입력합니다.
5. 다음 항목을 확인합니다.

```text
GAME=True
GAME_SOCKET=True
GAME_READY=True
SYNC=READY
SAVE=slot_...
CATALOG=1 이상
OVERLAY=ready
```

외형 목록은 현재 저장 파일에서 해금된 신도 형상을 게임으로부터 읽어옵니다. 새 게임이거나 아직 외형을 불러오지 못한 경우 시청자 페이지에 “외형 목록이 아직 업로드되지 않았습니다”라고 나올 수 있습니다. 저장 파일을 불러온 상태로 잠시 기다리거나 Companion에서 `refresh-forms`를 입력하세요.

## 8. 신도 추첨 사용법

### 자동 추첨

게임에서 새 신도를 받아 이름과 외형을 정하는 영입 화면에 들어가면 추첨이 자동 시작됩니다.

1. OBS 중앙에 `!신도` 참여 안내와 30초 제한시간이 표시됩니다.
2. 시청자가 치지직 채팅에 `!신도`를 입력합니다.
3. 종료 시 한 명을 무작위로 뽑습니다.
4. 당첨자의 닉네임과 저장 외형을 새 신도에게 적용합니다.
5. 저장 외형이 없거나 현재 허용되지 않는 외형이면 게임에서 사용할 수 있는 안전한 기본값이 적용될 수 있습니다.

### 수동 명령

```text
raffle start   추첨 시작
raffle draw    즉시 마감하고 당첨자 선정
raffle cancel  추첨 취소
```

일반 방송에서는 게임의 신도 영입 화면과 연결된 자동 추첨 사용을 권장합니다. 게임에서 연결된 모집 대상이 없는 상태로 수동 추첨하면 당첨자를 뽑더라도 게임 데이터가 변경되지 않을 수 있습니다.

## 9. 후원 이벤트 안내

1,000원 이상의 치지직 후원이 들어오면 금액대와 현재 위치에 맞는 효과가 무작위로 결정됩니다.

### 마을 및 일반 지역

- 신앙 증가 또는 감소
- 일부 또는 전체 신도의 포만도 증가·감소
- 신앙과 포만도가 함께 변하는 축복·방해
- 고액 구간의 전원 포식, 대축복 또는 대재난

### 던전

- 체력 회복 또는 비치명 피해
- 열정 충전
- 이동속도 증가 또는 감소
- 공격력 증가 또는 감소
- 모든 적에게 피해
- 고액 구간의 강한 복합 버프·디버프

로딩, 장면 전환, 일시정지, 대화 또는 컷신 중 후원이 도착하면 즉시 적용하지 않고 대기합니다. 플레이 가능한 상태가 되면 들어온 순서대로 적용하며, 그때의 실제 위치에 맞는 규칙을 사용합니다. 지속 효과의 OBS 타이머도 게임과 함께 멈추고 다시 시작합니다.

Companion이 치지직 후원을 이미 수신했다면 게임이 꺼져 있거나 일시적으로 연결이 끊겨도 로컬 발신함에 보존하고 재연결 후 다시 전달합니다. 같은 요청을 재전송해도 Mod가 최근 적용 영수증을 확인하므로 게임 효과를 다시 적용하지 않습니다. `status`의 `DONATION_OUTBOX`는 아직 최종 결과를 받지 못한 보존 건수입니다. 단, Companion 자체가 종료되어 치지직 세션에 연결되지 않은 동안 발생한 후원은 수신할 수 없으므로 방송 중에는 Companion을 계속 실행해야 합니다.

효과를 적용하는 정확한 순간에 게임 프로세스가 종료되면 실제 적용 여부를 안전하게 판별할 수 없습니다. 이 경우 중복 효과를 피하기 위해 해당 요청을 자동 재실행하지 않고 실패로 기록합니다.

방송 공지에는 다음 내용을 포함하는 것이 좋습니다.

> 1,000원 이상 후원은 Cult of the Lamb에 무작위 이벤트를 발생시킵니다. 도움뿐 아니라 신앙·포만도·체력·이동속도·공격력에 불리한 효과도 포함됩니다. 로딩이나 대화 중 들어온 이벤트는 게임 진행이 가능해진 뒤 순서대로 적용됩니다.

후원 패치 개발본에서는 오버레이 없이도 Companion 창에서 `donation status`로 수신·해석·적용·실패 건수와 대기열 상태를, `donation recent`로 최근 20건의 결과를 확인할 수 있습니다. `SESSION` 수치는 이번 실행 중의 집계이고, `RECENT`는 보관 중인 최근 이력입니다. 이력의 시각은 UTC이며 한국 시각은 9시간을 더합니다.

- `APPLIED`: Mod가 게임 적용 성공을 응답했습니다.
- `QUEUED`, `DELIVERING`, `AWAITING_RESULT`, `WAITING_GAME`: 저장·전달·결과 또는 게임 진행을 기다리고 있습니다.
- `PARSE_FAILED`, `FAILED`, `EXCLUDED`: 입력 오류, 처리 실패, 또는 후원 종류·채널·규칙에 따른 제외입니다. 함께 출력되는 사유를 확인하세요.
- `UNCERTAIN`: 중단이나 기록 누락으로 적용 여부를 확정할 수 없습니다. 중복 효과를 피하기 위해 자동 재실행하지 않습니다.
- 표시 상태 `PAGE_CONFIRMED`: 연결된 페이지가 해당 후원 카드를 표시했다고 응답했습니다. OBS의 실제 방송 송출 여부까지 증명하지는 않습니다.
- `WAITING_CLIENT`, `QUEUED`: 표시를 기다립니다. 표시 대기는 최대 100건·30분이며, 초과 시 `EXPIRED_CAPACITY` 또는 `EXPIRED_AGE`로 남습니다. 재연결 때는 결과 안내만 표시하며 게임 효과를 다시 실행하지 않습니다.

최근 처리 이력은 완료된 건 기준 최대 1,000건·30일 보관합니다. 재시작 후 복구된 안내에는 개인정보를 저장하지 않는 정책에 따라 이름 대신 ‘후원자’를 표시합니다. `DONATION_HISTORY_ERROR` 또는 `DONATION_OUTBOX_ERROR`가 `none`이 아니면 기록·저장에 문제가 있으므로 해당 결과를 함께 전달하세요. 손상된 후원 대기열이나 적용 기록은 임의 삭제하지 마세요.

이번 개발본의 자동 테스트 결과와 실제 치지직·게임·OBS에서 남은 검증은 [패치 검증 기록](../DONATION_PATCH_VALIDATION_KO.md)에 정리되어 있습니다.

## 10. 방송 시작 전 체크리스트

- [ ] 최신 Setup EXE로 설치 또는 업데이트했다.
- [ ] Companion 창에 `v1.0.0`과 `RELEASE / CHZZK LIVE`가 표시된다.
- [ ] 방송용 치지직 계정으로 로그인했다.
- [ ] Cult of the Lamb 저장 파일을 불러왔다.
- [ ] `status`에서 `GAME=True`, `SYNC=READY`를 확인했다.
- [ ] `status`에서 `DONATION_OUTBOX=0`인지 확인했다.
- [ ] OBS 브라우저 소스가 전체 캔버스 크기다.
- [ ] `status`에서 `OVERLAY=ready`, `OVERLAY_DOC_CURRENT=True`를 확인했다.
- [ ] `viewer open`으로 시청자 페이지가 열리고 외형을 저장할 수 있다.
- [ ] 시청자 링크와 `!신도` 명령을 공지했다.
- [ ] 후원에는 도움과 방해 효과가 모두 있다는 점을 공지했다.
- [ ] Companion 창을 방송 중 계속 켜 두었다.

## 11. 업데이트와 재설치

1. Cult of the Lamb과 Companion을 종료합니다.
2. 새 릴리스의 Setup EXE를 실행합니다.
3. 같은 게임 경로에 다시 설치합니다.
4. Companion을 실행하고 제목의 버전을 확인합니다.
5. 기존 HTTP URL 소스라면 OBS에서 로컬 파일 방식으로 한 번 전환합니다. 이미 로컬 파일 방식이라면 같은 경로가 유지되며 Companion 재실행 후 자동 연결됩니다. 로컬 연결 파일 자체가 업데이트된 경우에는 현재 페이지를 한 번 새로고침합니다.
6. `overlay`와 `status`로 파일 경로, 문서 버전과 연결 상태를 확인합니다.

재설치는 `%LOCALAPPDATA%\LambLink`의 사용자 설정과 시청자 매핑·외형 데이터를 삭제하지 않습니다.

## 12. 문제 해결과 지원 파일

### 설치가 실패할 때

- 게임과 Companion을 종료한 뒤 다시 시도합니다.
- 게임 경로에 `Cult Of The Lamb.exe`가 있는지 확인합니다.
- 인터넷 연결과 보안 프로그램의 차단 기록을 확인합니다.
- `%LOCALAPPDATA%\LambLink\installer.log`를 확인합니다.

### 게임과 연결되지 않을 때

- 게임에서 저장 파일을 불러와 플레이 화면까지 들어갑니다.
- Companion을 종료했다가 다시 실행합니다.
- 설치기를 다시 실행해 BepInEx, COTL_API와 Mod를 복구합니다.
- `Cult of the Lamb\BepInEx\LogOutput.log`를 확인합니다.

### 시청자 페이지에 외형이 없을 때

- 올바른 저장 파일을 불러왔는지 확인합니다.
- Companion에서 `refresh-forms`를 입력합니다.
- `status`의 `CATALOG` 값이 1 이상인지 확인합니다.
- `forms` 명령으로 허용 상태를 확인합니다.

### 진단 ZIP 만들기

Companion에서 `support`를 입력하면 바탕화면에 진단 ZIP이 생성됩니다. 자동으로 전송되지 않으므로 사용자가 내용을 확인한 뒤 개발자에게 직접 전달해야 합니다. 마지막 ZIP을 탐색기에서 찾으려면 `support open`을 입력합니다.

진단 ZIP은 OAuth 토큰, 설정 파일, 시청자 매핑·외형 내용과 게임 세이브를 포함하지 않도록 구성되어 있습니다. 그래도 외부에 공개 게시하기보다는 신뢰할 수 있는 지원 창구로 전달하는 것을 권장합니다.

### 로그 위치

```text
Companion: %LOCALAPPDATA%\LambLink\companion-1.0.0.log
설치기:    %LOCALAPPDATA%\LambLink\installer.log
게임 Mod:  Cult of the Lamb\BepInEx\LogOutput.log
```

## 13. 정상 종료

게임을 종료한 뒤 Companion 창에 `exit`를 입력하면 정상적으로 종료됩니다. 콘솔 창의 닫기 버튼으로 종료해도 되지만, 가능하면 `exit` 명령을 사용하세요.
